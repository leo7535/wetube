extends layouts/main
include mixins/videoBlock

block content
    .videos
        each item in movies
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                url:item.url
            })