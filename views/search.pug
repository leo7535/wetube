extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching for #{searchingBy}
    .search__videos
        each items in videos
            +videoBlock({
                title:items.title,
                views:items.views,
                url:items.url
            })